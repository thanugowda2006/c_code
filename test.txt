#include <stdio.h>

int main(void) {
    // Print Hello World to the console with a newline
    printf("Hello, World!\n");
    return 0;
#include <stdio.h>

int main(void) {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    if (num % 2 == 0)
        printf("%d is even.\n", num);
    else
        printf("%d is odd.\n", num);
    return 0;
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}}}
